{
  "name": "example",
  "private": true,
  "type": "module",
  "scripts": {
    "bun": "bun test ./bun",
    "vitest": "vitest run ./vitest --isolate=false --pool=threads --max-workers=32 --experimental.vite-module-runner=false"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
