{
  "name": "example",
  "private": true,
  "type": "module",
  "scripts": {
    "bun": "bun test --parallel=32 ./bun",
    "vitest": "vitest run ./vitest --reporter minimal --isolate=false --pool=threads --max-workers=32 --experimental.vite-module-runner=false"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
